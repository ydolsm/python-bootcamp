# Implement the given function

def counter(items):
    """Return a new dictionary:
        Keys -> item in items
        Values -> number of item in items (count)

    Example:
        items = [1, 1, 2, 2, 2, 3]
        item_count = counter(items)
        print(item_count)

        >> {1: 2, 2: 3, 3: 1}
    """

    return dict()

# Try this example:
items = [1, 1, 2, 2, 2, 3]
item_count = counter(items)
print(item_count)